# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# Node (none required, but keep installs out)
node_modules/
.npmcache/

# Build output: the single-file bundle generated by `npm run build`
# (the source of truth lives in src/, so the built root index.html is a build artifact)
/index.html

# Local server / deploy tooling
dist/
build/
public/

# Environment / secrets
.env
.env.*
*.pem

# Tests (local harness only)
/tmp/opencode/
