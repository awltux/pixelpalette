# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# Node (none required, but keep installs out)
node_modules/
.npmcache/

# Build output: the generated bundle written by `npm run build` into dist/.
# The source of truth lives in src/, so dist/ is regenerable.
dist/

# Local server / deploy tooling
build/
public/

# Environment / secrets
.env
.env.*
*.pem

# Tests (local harness only)
/tmp/opencode/
